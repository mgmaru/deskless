# deskless notes

## ドキュメント概要
desklessプロジェクトで分かったことや思ったことを雑多に記載する。

## メモ
### 2026-9-10
### やったこと
- アーキテクチャの検討
### 分かったこと
- tmuxは、エージェントを常駐させるためのツール。
- エージェントをDockerに閉じ込めることでWSLのditroの環境を守ること。ルート権限を渡さない。
  - コンテナからdistroは見えないのを利用。
  - 最悪、`rm`してもコンテナが消えるだけ。distroは消えない。
- WSLにクローンしたリポジトリを、Dockerのコンテナ内にマウントする。
  - 私はdistro内のリポジトリファイルを編集し、エージェントはDockerにマウントされたファイルを編集する。
    - 誤解してはいけないのは、２つのファイルはどちらも同じもの。
    - ユーザとエージェントが別経路で１つのファイルを編集しているだけ。
### 疑問
- WSLのdistroのファイルをDockerにマウントすると、distro側のファイルが変更されたら、Dockerも変更されると思います。
  - しかし例えば、Dockerを停止している状態で、distroのファイルを変更した場合、Docker内部のファイルは書き換えられないと思います。
  - この場合、再度Dockerを起動すると、マウントしているファイルは同期されるのでしょうか？
  - また、もし同期されるとき、しっかり、WSLの変更がDocker側へ流れるという認識で良いですよね？間違っても、Dockerのファイル状態がdistroに流れるということはないですよね？
  - 回答：そもそも**Docker内部にファイルの実体はない。**ファイルの実態はあくまでdistro側であり、Dockerが停止して起動したら、再度、WSLのファイルがDockerコンテナにマウントするだけ。
---
